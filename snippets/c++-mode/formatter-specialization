# -*- mode: snippet -*-
# name: formatter-specialization
# key: fmts
# group: cpp20
# --
/// @brief Custom formatter for ${1:Type}
template <>
struct std::formatter<$1> : std::formatter<std::string_view> {
    auto format($1 const& ${2:val}, std::format_context& ctx) const
    {
        return std::formatter<std::string_view>::format(
            std::format("${3:{}}", ${0:val.field}),
            ctx
        );
    }
};
