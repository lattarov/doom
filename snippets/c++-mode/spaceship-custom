# -*- mode: snippet -*-
# name: spaceship-custom
# key: spacec
# group: cpp20
# --
/// @brief Custom three-way comparison
auto operator<=>(${1:Type} const& ${2:other}) const
    -> std::${3:strong_ordering}
{
    if (auto cmp = ${4:field_} <=> $2.$4; cmp != 0) {
        return cmp;
    }
    return ${5:other_field_} <=> $2.$5;
}

auto operator==($1 const& $2) const -> bool
{
    return (*this <=> $2) == 0;
}
$0
